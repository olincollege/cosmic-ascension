[tool.black]
line-length = 80
preview = true

[tool.pylint.main]

extension-pkg-allow-list = "pygame"

[tool.pylint.format]
max-line-length = 80

[tool.pylint."messages control"]
disable = ["c-extension-no-member"]